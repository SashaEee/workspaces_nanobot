"""Общие фрагменты промптов, используемые всеми агентами.

`FOLLOWUPS_INSTRUCTION` дописывается в конец системного промпта каждого агента,
чтобы LLM генерировала follow-up чипы в строго определённом формате.

`HISTORY_BLOCK_TEMPLATE` подставляется в начало user-промпта, чтобы агент
понимал контекст прошлого диалога (для drill-down, переспросов и т.п.).
"""

FOLLOWUPS_INSTRUCTION = """

ПОСЛЕ ОСНОВНОГО ОТВЕТА — ВСЕГДА выдай блок-сентинел с 3 короткими (до 80 символов)
follow-up подсказками, которые помогут аудитору углубиться. Подсказки — это конкретные
вопросы/команды, которые он мог бы задать следующим ходом, исходя из ТВОЕГО ответа
и того, что есть в контексте.

ФОРМАТ строго такой (НЕ переводи теги, НЕ меняй кавычки):
<followups>["вопрос 1", "вопрос 2", "вопрос 3"]</followups>

Подсказки должны:
- быть конкретными (упоминать КМ, тему, аспект — а не «расскажи больше»);
- ссылаться на КМ из контекста, если они там есть;
- если уместно — предлагать drill-down: «какие базы анализировались в КМ-…»,
  «покажи цитаты из КМ-…», «какие подразделения проверяли в КМ-…».
"""

HISTORY_BLOCK_TEMPLATE = """ИСТОРИЯ ДИАЛОГА (для понимания контекста и местоимений):
{history}

"""
