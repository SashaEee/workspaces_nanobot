"""Follow Up как навык внешнего агента (единое окно подразделения).

Здесь нет ни второго конвейера ответа, ни второго набора правил. Навык
гоняет ТОТ ЖЕ ход, что и собственный интерфейс, и собирает из его событий
плоский результат. Иначе два пути разошлись бы: тот, что чаще правят,
уезжает вперёд, а второй тихо отвечает по-старому.

Наружу выставлены целые навыки, а не сырые инструменты поиска
(`core/tools/registry.py`). Сырые не знают ни контракта ответа, ни критика
достаточности, ни границ проверки: отдав их чужой модели, мы отдали бы
право подставить акт одной КМ в ответ про другую. План — в
`docs/NANOBOT_INTEGRATION_PLAN.md`, раздел 3.

Транспорта здесь тоже нет. Функции чистые: принимают строки, возвращают
словари. Поверх них ложится что угодно — MCP-сервер, CLI, HTTP.
"""
