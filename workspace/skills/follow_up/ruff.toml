# Код Follow Up (backend/) проверяется линтером в собственном репозитории
# Follow Up и приезжает сюда готовым; здесь — только то, что написано
# под агента. Остальные настройки — из корневого pyproject.toml.
extend = "../../../pyproject.toml"
extend-exclude = ["backend"]
